import { Canvas, Meta, ArgsTable, Story } from '@storybook/addon-docs';
import { MemoryRouter as Router } from 'react-router-dom';

import Banner, { BannerType } from './Banner';

<Meta
  title='Molecule/Banner'
  component={Banner}
  parameters={{
    viewMode: 'docs',
    layout: 'fullscreen',
  }}
/>

# Banner

export const Template = (args) => {
  return <Banner {...args}>{`${args.type ?? BannerType.INFORMATION} message`}</Banner>;
};

<Canvas withSource='open'>
  <Story name='Banner'>{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={Banner} sort='requiredFirst' />

### Warning

<Canvas withSource='open'>
  <Banner type={BannerType.WARNING}>Warning message</Banner>
</Canvas>

### Error

<Canvas withSource='open'>
  <Banner type={BannerType.ERROR}>Error message</Banner>
</Canvas>
