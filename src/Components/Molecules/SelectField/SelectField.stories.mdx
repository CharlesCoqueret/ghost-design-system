import { useState } from 'react';
import { Canvas, Meta, ArgsTable, Story } from '@storybook/addon-docs';

import SelectField from './SelectField';

<Meta
  title='Molecule/SelectField'
  component={SelectField}
  parameters={{
    viewMode: 'docs',
    layout: 'fullscreen',
  }}
  argTypes={{
    dataTestId: {
      table: {
        disable: true,
      },
    },
  }}
/>

# SelectField

export const selectedOptions = {
  label: 'selection label 2 - which can sometimes be ' + 'very '.repeat(10) + ' long',
  value: 'KEY_2',
};

export const options = [
  { label: 'selection label 0', value: 'KEY_0' },
  { label: 'selection label 1', value: 'KEY_1' },
  selectedOptions,
  {
    label: 'selection label 3 - which can sometimes be ' + 'extremely '.repeat(50) + ' long',
    value: 'KEY_3',
  },
  { label: 'selection label 4', value: 'KEY_4' },
  { label: 'selection label 5', value: 'KEY_5' },
  { label: 'selection label 6', value: 'KEY_6' },
  { label: 'selection label 7', value: 'KEY_7' },
  { label: 'selection label 8', value: 'KEY_8' },
  { label: 'selection label 9', value: 'KEY_9' },
  { label: 'selection label 10', value: 'KEY_10' },
];

export const Template = (args) => {
  const [localValue, setLocalValue] = useState(args.inputValue);
  return <SelectField {...args} inputValue={localValue || ''} onChange={setLocalValue} />;
};

<Canvas>
  <Story
    name='SelectField'
    args={{
      isClearable: true,
      label: 'Select field',
      options: options,
      placeholder: 'Select placeholder',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={SelectField} sort='requiredFirst' exclude={['dataTestId']} />

### Read Only

<Canvas>
  <Template
    fieldSize={6}
    label='Select field in read only with label size = 4 and field size = 6'
    labelSize={4}
    options={options}
    placeholder='Select placeholder'
    readOnly={true}
    inputValue={selectedOptions.value}
  />
</Canvas>

### Error

<Canvas>
  <Template
    fieldSize={6}
    label='Select field in error with label size = 4 and field size = 6'
    labelSize={4}
    options={options}
    placeholder='Select placeholder'
    errorMessage='This text is on error'
  />
</Canvas>

### Helper

<Canvas>
  <Template
    helperText='Helper text'
    label='Select field with helper and counter'
    mandatory={true}
    options={options}
    placeholder='Select placeholder'
  />
</Canvas>

### Highlighted

<Canvas>
  <Template
    helperText='Helper text'
    highlighted={true}
    label='Select field highlighted'
    options={options}
    placeholder='Select placeholder'
    readOnly={true}
    inputValue={selectedOptions.value}
  />
</Canvas>

### Disabled

<Canvas>
  <Template
    disabled={true}
    helperText='Helper text'
    label='Select field disabled'
    options={options}
    placeholder='Select placeholder'
    inputValue={selectedOptions.value}
  />
</Canvas>
